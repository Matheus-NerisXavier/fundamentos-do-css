# 🎨 Fundamentos do CSS - Rocketseat

Este repositório contém anotações, exercícios e projetos práticos desenvolvidos durante o curso de **Fundamentos do CSS** da [Rocketseat](https://www.rocketseat.com.br/). O foco aqui é dominar a estilização de páginas web, compreendendo desde a sintaxe básica até conceitos avançados de posicionamento e design responsivo.

## 📁 Estrutura do Repositório

As pastas estão organizadas pelos principais pilares da estilização:

* **`anatomia-e-seletores/`**: Entendendo a sintaxe do CSS (propriedade e valor) e como selecionar elementos via tags, classes, IDs e seletores compostos.
* **`box-model/`**: Estudo fundamental sobre o modelo de caixa: `content`, `padding`, `border` e `margin`, além do uso de `box-sizing`.
* **`cores-e-background/`**: Trabalhando com cores (Hex, RGB, HSL), gradientes e imagens de fundo.
* **`tipografia/`**: Estilização de textos, importação de fontes (Google Fonts), `font-size`, `line-height` e alinhamentos.
* **`unidades-de-medida/`**: Diferença entre unidades fixas (`px`) e relativas (`em`, `rem`, `%`, `vw`, `vh`).
* **`cascata-e-especificidade/`**: Como o CSS resolve conflitos de estilos e a importância da hierarquia de seletores.
* **`posicionamento/`**: Práticas com `static`, `relative`, `absolute` e `fixed`.

## 🚀 O que foi aprendido

Neste módulo, os principais conceitos abordados foram:
- Como vincular o CSS ao HTML (Inline, Internal e External).
- O poder dos seletores e pseudo-classes (`:hover`, `:nth-child`, etc).
- Manipulação visual de elementos para criar layouts atraentes.
- Boas práticas de organização de código CSS.
- Introdução a conceitos que preparam para o uso de Flexbox e Grid.

## 🛠 Tecnologias e Ferramentas

- **HTML5**: Estrutura das páginas para estilização.
- **CSS3**: Estilização avançada e moderna.
- **VS Code**: Editor de código principal.
- **Live Server**: Para visualização em tempo real das alterações.

## 💻 Como utilizar

1. Clone o repositório:
   ```bash
   git clone [https://github.com/Matheus-NerisXavier/fundamentos-do-css.git](https://github.com/Matheus-NerisXavier/fundamentos-do-css.git)